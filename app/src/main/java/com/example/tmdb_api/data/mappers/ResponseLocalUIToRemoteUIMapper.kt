package com.example.tmdb_api.data.mappers

import javax.inject.Inject

class ResponseLocalUIToRemoteUIMapper
    @Inject
    constructor() {
    /*
    fun mapShowList(responseRemote: List<ResponseLocalUI>): List<ResponseRemoteUI> {
        return responseRemote.map {
            ResponseRemoteUI(
                it.id,
                it.imdbID,
                it.title,
                it.overview,
                it.releaseYear,
                it.originalTitle,
                it.genres,
                it.directors,
                it.cast,
                it.rating,
                it.imageSet,
                it.streamingOptions,
                it.seasonCount


            )
        }
    }

    fun mapShow(responseRemote: ResponseLocalUI): ResponseRemoteUI {
        return ResponseRemoteUI(
            responseRemote.id,
            responseRemote.imdbID,
            responseRemote.title,
            responseRemote.overview,
            responseRemote.releaseYear,
            responseRemote.originalTitle,
            responseRemote.genres,
            responseRemote.directors,
            responseRemote.cast,
            responseRemote.rating,
            responseRemote.imageSet,
            responseRemote.streamingOptions,
            responseRemote.seasonCount
        )
    }

     */
    }
